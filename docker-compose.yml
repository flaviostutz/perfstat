version: '3.5'

services:

  perfstat:
    build: .
    environment:
      - RUN_TESTS=false
    volumes:
      - ./:/app
      - ./dist:/dist
    privileged: true
    stdin_open: true
    tty: true
    ports: 
      - 8880:8880
    # command: sleep 99999

